@model ExpenseFormViewModel
@{
    var isCreate = Model.Id == 0;
    var selectedCarLabel = Model.CarOptions.FirstOrDefault(option => option.Value == Model.CarId.ToString())?.Text ?? string.Empty;
    var selectedCarValue = Model.CarId > 0 ? Model.CarId.ToString() : string.Empty;
    var carLookupUrl = Url.Action("Search", "Lookups", new { source = "cars" }) ?? string.Empty;
    var selectedCategoryLabel = Model.CategoryOptions.FirstOrDefault(option => option.Value == Model.CategoryId.ToString())?.Text ?? string.Empty;
    var selectedCategoryValue = Model.CategoryId > 0 ? Model.CategoryId.ToString() : string.Empty;
    var categoryLookupUrl = Url.Action("Search", "Lookups", new { source = "categories" }) ?? string.Empty;
}

<main class="auto-page-container">
    <nav class="auto-breadcrumb" aria-label="Back navigation">
        <a asp-controller="Expenses" asp-action="Index" class="auto-btn auto-btn-ghost">Back to Expenses</a>
    </nav>

    <section class="auto-form-container">
        <header class="auto-form-header">
            <div>
                <p class="auto-kicker">Expense ledger</p>
                <h1 class="auto-form-title">@(isCreate ? "Add New Expense" : "Edit Expense")</h1>
            </div>
        </header>

        <div class="auto-form-shell">
            <form asp-action="@(isCreate ? "Create" : "Edit")" asp-route-id="@Model.Id" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger auto-validation-summary"></div>
                <div class="auto-form-grid">
                    <div class="auto-form-group auto-form-group-wide">
                        <label class="auto-label" for="CarId_Display">@Html.DisplayNameFor(model => model.CarId)</label>
                        @await Html.PartialAsync("_AutocompleteDropdown", new AutocompleteDropdownViewModel
                        {
                            InputId = "CarId_Display",
                            HiddenInputId = "CarId",
                            HiddenInputName = "CarId",
                            LookupUrl = carLookupUrl,
                            Placeholder = "Search car",
                            SelectedLabel = selectedCarLabel,
                            SelectedValue = selectedCarValue,
                            MinChars = 1,
                            Limit = 25
                        })
                        <span asp-validation-for="CarId" class="text-danger"></span>
                    </div>
                    <div class="auto-form-group auto-form-group-wide">
                        <label class="auto-label" for="CategoryId_Display">@Html.DisplayNameFor(model => model.CategoryId)</label>
                        @await Html.PartialAsync("_AutocompleteDropdown", new AutocompleteDropdownViewModel
                        {
                            InputId = "CategoryId_Display",
                            HiddenInputId = "CategoryId",
                            HiddenInputName = "CategoryId",
                            LookupUrl = categoryLookupUrl,
                            Placeholder = "Search category",
                            SelectedLabel = selectedCategoryLabel,
                            SelectedValue = selectedCategoryValue,
                            MinChars = 1,
                            Limit = 25
                        })
                        <span asp-validation-for="CategoryId" class="text-danger"></span>
                    </div>
                    <div class="auto-form-group auto-form-group-wide">
                        <label asp-for="Description" class="auto-label"></label>
                        <textarea asp-for="Description" class="auto-input" rows="4"></textarea>
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>
                    <div class="auto-form-group">
                        <label asp-for="Amount" class="auto-label"></label>
                        <input asp-for="Amount" class="auto-input" type="number" step="0.01" />
                        <span asp-validation-for="Amount" class="text-danger"></span>
                    </div>
                    <div class="auto-form-group">
                        <label asp-for="Date" class="auto-label"></label>
                        <input asp-for="Date" class="auto-input" type="date" />
                        <span asp-validation-for="Date" class="text-danger"></span>
                    </div>
                </div>

                <footer class="auto-form-actions">
                    <button type="submit" class="auto-btn auto-btn-accent">Save Expense</button>
                    <a asp-controller="Expenses" asp-action="Index" class="auto-btn auto-btn-ghost">Cancel</a>
                </footer>
            </form>
        </div>
    </section>
</main>
