@model Expense

<main class="auto-page-container">
    <nav class="auto-breadcrumb" aria-label="Back navigation">
        <a asp-controller="Expenses" asp-action="Index" class="auto-btn auto-btn-ghost">Back to Expenses</a>
    </nav>

    <article class="auto-details-hero auto-card-danger">
        <header class="auto-details-header auto-details-header-danger">
            <div>
                <p class="auto-kicker">Destructive action</p>
                <h1 class="auto-hero-title">Delete Expense #@Model.Id?</h1>
            </div>
            <span class="auto-hero-tag">@Model.Amount.ToString("C")</span>
        </header>

        <div class="auto-details-body auto-delete-body">
            <section class="auto-data-panel">
                <h2 class="auto-panel-heading">Expense summary</h2>
                <dl class="auto-stats auto-stats-compact">
                    <dt>Category</dt>
                    <dd>@Model.Category.Name</dd>
                    <dt>Date</dt>
                    <dd>@Model.Date.ToString("yyyy-MM-dd")</dd>
                </dl>
            </section>

            <aside class="auto-action-panel auto-danger-panel">
                <h3 class="auto-panel-heading">Confirm deletion</h3>
                <p class="auto-danger-copy">This will soft-delete the expense.</p>
                <form asp-action="Delete" asp-route-id="@Model.Id" method="post" class="auto-form-actions auto-form-actions-inline">
                    <button type="submit" class="auto-btn auto-btn-danger">Delete Expense</button>
                    <a asp-controller="Expenses" asp-action="Index" class="auto-btn auto-btn-ghost">Cancel</a>
                </form>
            </aside>
        </div>
    </article>
</main>
