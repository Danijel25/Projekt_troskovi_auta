@model ExpenseCategoryFormViewModel
@{
    var isCreate = Model.Id == 0;
}

<main class="auto-page-container">
    <nav class="auto-breadcrumb" aria-label="Back navigation">
        <a asp-controller="ExpenseCategories" asp-action="Index" class="auto-btn auto-btn-ghost">Back to Categories</a>
    </nav>

    <section class="auto-form-container">
        <header class="auto-form-header">
            <div>
                <p class="auto-kicker">Category management</p>
                <h1 class="auto-form-title">@(isCreate ? "Add New Category" : "Edit Category")</h1>
            </div>
        </header>

        <div class="auto-form-shell">
            <form asp-action="@(isCreate ? "Create" : "Edit")" asp-route-id="@Model.Id" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger auto-validation-summary"></div>
                <div class="auto-form-grid">
                    <div class="auto-form-group auto-form-group-wide">
                        <label asp-for="Name" class="auto-label"></label>
                        <input asp-for="Name" class="auto-input" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                </div>

                <footer class="auto-form-actions">
                    <button type="submit" class="auto-btn auto-btn-accent">Save Category</button>
                    <a asp-controller="ExpenseCategories" asp-action="Index" class="auto-btn auto-btn-ghost">Cancel</a>
                </footer>
            </form>
        </div>
    </section>
</main>
