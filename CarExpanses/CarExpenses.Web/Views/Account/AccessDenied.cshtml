@{
    ViewData["Title"] = "Access denied";
}

<main class="auto-page-container">
    <section class="auto-form-container">
        <header class="auto-form-header">
            <div>
                <p class="auto-kicker">Restricted</p>
                <h1 class="auto-form-title">Access denied</h1>
            </div>
        </header>
        <div class="auto-form-shell">
            <p class="auto-empty-state">You do not have permission to view this page.</p>
            <div class="auto-form-actions">
                <a asp-controller="Home" asp-action="Index" class="auto-btn auto-btn-ghost">Back home</a>
            </div>
        </div>
    </section>
</main>
